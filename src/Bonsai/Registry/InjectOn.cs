namespace Bonsai.Registry
{
    /// <summary>
    /// these are the different way to inject into an object
    /// </summary>
    public enum InjectOn
    {
        Constructor,
        Property,
        Method
    }
}